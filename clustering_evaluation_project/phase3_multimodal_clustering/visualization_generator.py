"""
Generador de Visualizaciones Científicas para Sistema Clustering Multimodal
==========================================================================

Implementa generación de visualizaciones especializadas para análisis
de clustering multimodal, matrices de correspondencia, y comparaciones
algorítmicas con enfoque en interpretabilidad científica.

Autor: Proyecto FASE 3 - Sistema Clustering Multimodal
Fecha: Agosto 2025
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
from typing import Dict, List, Any, Tuple, Optional, Union
from pathlib import Path
import json
import warnings
import logging

# Configuración de estilo científico
plt.style.use('seaborn-v0_8-whitegrid' if 'seaborn-v0_8-whitegrid' in plt.style.available else 'default')
sns.set_palette(\"husl\")\n\n\nclass VisualizationGenerator:\n    \"\"\"\n    Generador especializado de visualizaciones para clustering multimodal.\n    \n    Crea visualizaciones científicas para análisis de resultados,\n    comparaciones algorítmicas, y interpretabilidad de correspondencias.\n    \"\"\"\n    \n    def __init__(self, output_directory: str, figure_format: str = 'png', \n                 figure_dpi: int = 300, verbose: bool = True):\n        \"\"\"\n        Inicializar generador de visualizaciones.\n        \n        Args:\n            output_directory: Directorio para guardar visualizaciones\n            figure_format: Formato de figuras ('png', 'pdf', 'svg')\n            figure_dpi: DPI para figuras\n            verbose: Activar logging detallado\n        \"\"\"\n        self.output_directory = Path(output_directory)\n        self.output_directory.mkdir(parents=True, exist_ok=True)\n        \n        self.figure_format = figure_format\n        self.figure_dpi = figure_dpi\n        self.verbose = verbose\n        \n        # Configurar logging\n        self.logger = self._setup_logging()\n        \n        # Configuración de estilo\n        self.style_config = {\n            'figure_size': (12, 8),\n            'title_fontsize': 16,\n            'label_fontsize': 12,\n            'tick_fontsize': 10,\n            'legend_fontsize': 11,\n            'color_palette': sns.color_palette(\"husl\", 12)\n        }\n        \n        self.logger.info(f\"VisualizationGenerator inicializado: {self.output_directory}\")\n    \n    def _setup_logging(self) -> logging.Logger:\n        \"\"\"Configurar logging para generador.\"\"\"\n        logger = logging.getLogger(f'VisualizationGenerator_{id(self)}')\n        logger.setLevel(logging.INFO if self.verbose else logging.WARNING)\n        \n        if not logger.handlers:\n            handler = logging.StreamHandler()\n            formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')\n            handler.setFormatter(formatter)\n            logger.addHandler(handler)\n        \n        return logger\n    \n    def generate_algorithm_comparison_chart(self, experiment_results: Dict[str, Any], \n                                          domain: str) -> str:\n        \"\"\"\n        Generar gráfico comparativo de algoritmos por dominio.\n        \n        Args:\n            experiment_results: Resultados completos de experimentación\n            domain: 'musical' o 'semantic'\n            \n        Returns:\n            Ruta al archivo generado\n        \"\"\"\n        self.logger.info(f\"Generando comparación algorítmica para dominio {domain}\")\n        \n        domain_experiments = experiment_results.get(f'{domain}_experiments', [])\n        \n        if not domain_experiments:\n            self.logger.warning(f\"No hay experimentos disponibles para dominio {domain}\")\n            return \"\"\n        \n        # Preparar datos para visualización\n        plot_data = []\n        \n        for exp in domain_experiments:\n            if not exp.get('error', False):\n                plot_data.append({\n                    'Algorithm': exp.get('algorithm_name', 'Unknown'),\n                    'K': exp.get('k_effective', 0),\n                    'Silhouette': exp.get('silhouette_score', 0),\n                    'Balance': exp.get('balance_distribution_score', 0),\n                    'Interpretability': exp.get('interpretability_score', 0),\n                    'Composite_Score': exp.get('composite_score_partial', 0)\n                })\n        \n        if not plot_data:\n            self.logger.warning(f\"No hay datos válidos para visualización {domain}\")\n            return \"\"\n        \n        df = pd.DataFrame(plot_data)\n        \n        # Crear figura con subplots\n        fig, ((ax1, ax2), (ax3, ax4)) = plt.subplots(2, 2, figsize=(15, 12))\n        fig.suptitle(f'Análisis Comparativo de Algoritmos - Dominio {domain.title()}', \n                     fontsize=16, fontweight='bold')\n        \n        # 1. Silhouette Score por Algoritmo\n        sns.boxplot(data=df, x='Algorithm', y='Silhouette', ax=ax1)\n        ax1.set_title('Distribución Silhouette Score')\n        ax1.set_xlabel('Algoritmo')\n        ax1.set_ylabel('Silhouette Score')\n        ax1.tick_params(axis='x', rotation=45)\n        \n        # 2. Balance vs Interpretabilidad\n        scatter = ax2.scatter(df['Balance'], df['Interpretability'], \n                             c=df['Composite_Score'], cmap='viridis', \n                             s=60, alpha=0.7)\n        ax2.set_title('Balance vs Interpretabilidad')\n        ax2.set_xlabel('Score Balance Distribución')\n        ax2.set_ylabel('Score Interpretabilidad')\n        plt.colorbar(scatter, ax=ax2, label='Score Compuesto')\n        \n        # 3. Score Compuesto por K\n        for algorithm in df['Algorithm'].unique():\n            alg_data = df[df['Algorithm'] == algorithm]\n            ax3.plot(alg_data['K'], alg_data['Composite_Score'], \n                    marker='o', label=algorithm, linewidth=2)\n        \n        ax3.set_title('Score Compuesto vs Número de Clusters')\n        ax3.set_xlabel('Número de Clusters (K)')\n        ax3.set_ylabel('Score Compuesto')\n        ax3.legend(bbox_to_anchor=(1.05, 1), loc='upper left')\n        ax3.grid(True, alpha=0.3)\n        \n        # 4. Heatmap de métricas normalizadas\n        metrics_matrix = df.pivot_table(\n            index='Algorithm', \n            values=['Silhouette', 'Balance', 'Interpretability'], \n            aggfunc='mean'\n        ).fillna(0)\n        \n        sns.heatmap(metrics_matrix.T, annot=True, fmt='.3f', \n                   cmap='RdYlBu_r', ax=ax4, cbar_kws={'label': 'Score Normalizado'})\n        ax4.set_title('Mapa de Calor - Métricas Promedio')\n        ax4.set_xlabel('Algoritmo')\n        ax4.set_ylabel('Métrica')\n        \n        plt.tight_layout()\n        \n        # Guardar figura\n        filename = f'algorithm_comparison_{domain}_{pd.Timestamp.now().strftime(\"%Y%m%d_%H%M%S\")}.{self.figure_format}'\n        filepath = self.output_directory / filename\n        plt.savefig(filepath, dpi=self.figure_dpi, bbox_inches='tight')\n        plt.close()\n        \n        self.logger.info(f\"Comparación algorítmica guardada: {filepath}\")\n        return str(filepath)\n    \n    def generate_cross_modal_correspondence_matrix(self, cross_modal_results: Dict[str, Any]) -> str:\n        \"\"\"\n        Generar matriz de correspondencia cross-modal.\n        \n        Args:\n            cross_modal_results: Resultados de análisis cross-modal\n            \n        Returns:\n            Ruta al archivo generado\n        \"\"\"\n        self.logger.info(\"Generando matriz de correspondencia cross-modal\")\n        \n        correspondences = cross_modal_results.get('correspondences', [])\n        \n        if not correspondences:\n            self.logger.warning(\"No hay correspondencias cross-modal disponibles\")\n            return \"\"\n        \n        # Crear figura con múltiples visualizaciones\n        fig, ((ax1, ax2), (ax3, ax4)) = plt.subplots(2, 2, figsize=(16, 12))\n        fig.suptitle('Análisis de Correspondencias Cross-Modal', fontsize=16, fontweight='bold')\n        \n        # 1. Score de correspondencia por combinación\n        corr_data = []\n        for corr in correspondences[:10]:  # Top 10 correspondencias\n            musical_alg = corr.get('musical_algorithm', 'Unknown')\n            semantic_alg = corr.get('semantic_algorithm', 'Unknown')\n            score = corr.get('correspondence_score', 0)\n            nmi = corr.get('normalized_mutual_info', 0)\n            \n            corr_data.append({\n                'Combination': f\"{musical_alg[:10]} ↔ {semantic_alg[:10]}\",\n                'Correspondence_Score': score,\n                'NMI': nmi\n            })\n        \n        if corr_data:\n            corr_df = pd.DataFrame(corr_data)\n            \n            # Gráfico de barras de scores\n            bars = ax1.bar(range(len(corr_df)), corr_df['Correspondence_Score'], \n                          color=self.style_config['color_palette'][:len(corr_df)])\n            ax1.set_title('Top Scores de Correspondencia Cross-Modal')\n            ax1.set_xlabel('Combinación Algorítmica')\n            ax1.set_ylabel('Score Correspondencia')\n            ax1.set_xticks(range(len(corr_df)))\n            ax1.set_xticklabels(corr_df['Combination'], rotation=45, ha='right')\n            \n            # Agregar valores en barras\n            for bar, score in zip(bars, corr_df['Correspondence_Score']):\n                height = bar.get_height()\n                ax1.text(bar.get_x() + bar.get_width()/2., height + 0.01,\n                        f'{score:.3f}', ha='center', va='bottom', fontsize=9)\n            \n            # 2. NMI vs Correspondence Score\n            scatter = ax2.scatter(corr_df['NMI'], corr_df['Correspondence_Score'], \n                                s=100, alpha=0.7, c=range(len(corr_df)), cmap='viridis')\n            ax2.set_title('NMI vs Score de Correspondencia')\n            ax2.set_xlabel('Normalized Mutual Information')\n            ax2.set_ylabel('Score de Correspondencia')\n            ax2.grid(True, alpha=0.3)\n            \n            # Línea de referencia diagonal\n            max_val = max(corr_df['NMI'].max(), corr_df['Correspondence_Score'].max())\n            ax2.plot([0, max_val], [0, max_val], 'r--', alpha=0.5, label='Referencia Ideal')\n            ax2.legend()\n        \n        # 3. Distribución de correspondencias fuertes\n        strong_correspondences = []\n        for corr in correspondences:\n            strong_corr_list = corr.get('strong_correspondences', [])\n            for sc in strong_corr_list:\n                strong_correspondences.append({\n                    'Musical_Cluster': sc.get('musical_cluster', 0),\n                    'Semantic_Cluster': sc.get('semantic_cluster', 0),\n                    'Strength': sc.get('correspondence_strength', 0),\n                    'Classification': sc.get('classification', 'unknown')\n                })\n        \n        if strong_correspondences:\n            strong_df = pd.DataFrame(strong_correspondences)\n            \n            # Histograma de strength\n            ax3.hist(strong_df['Strength'], bins=20, alpha=0.7, \n                    color=self.style_config['color_palette'][2], edgecolor='black')\n            ax3.set_title('Distribución de Strength de Correspondencias')\n            ax3.set_xlabel('Correspondence Strength (%)')\n            ax3.set_ylabel('Frecuencia')\n            ax3.grid(True, alpha=0.3)\n            \n            # 4. Clasificación de correspondencias\n            classification_counts = strong_df['Classification'].value_counts()\n            wedges, texts, autotexts = ax4.pie(classification_counts.values, \n                                              labels=classification_counts.index,\n                                              autopct='%1.1f%%',\n                                              colors=self.style_config['color_palette'][:len(classification_counts)])\n            ax4.set_title('Distribución de Clasificaciones')\n            \n            # Mejorar apariencia del pie chart\n            for autotext in autotexts:\n                autotext.set_color('white')\n                autotext.set_fontweight('bold')\n        else:\n            ax3.text(0.5, 0.5, 'No hay correspondencias\\nfuertes disponibles', \n                    ha='center', va='center', transform=ax3.transAxes, fontsize=12)\n            ax3.set_title('Distribución de Correspondencias')\n            \n            ax4.text(0.5, 0.5, 'No hay datos de\\nclasificación disponibles',\n                    ha='center', va='center', transform=ax4.transAxes, fontsize=12)\n            ax4.set_title('Clasificación de Correspondencias')\n        \n        plt.tight_layout()\n        \n        # Guardar figura\n        filename = f'cross_modal_correspondence_{pd.Timestamp.now().strftime(\"%Y%m%d_%H%M%S\")}.{self.figure_format}'\n        filepath = self.output_directory / filename\n        plt.savefig(filepath, dpi=self.figure_dpi, bbox_inches='tight')\n        plt.close()\n        \n        self.logger.info(f\"Matriz correspondencia guardada: {filepath}\")\n        return str(filepath)\n    \n    def generate_interpretability_analysis_chart(self, interpretability_results: Dict[str, Any]) -> str:\n        \"\"\"\n        Generar análisis de interpretabilidad multimodal.\n        \n        Args:\n            interpretability_results: Resultados de análisis de interpretabilidad\n            \n        Returns:\n            Ruta al archivo generado\n        \"\"\"\n        self.logger.info(\"Generando análisis de interpretabilidad\")\n        \n        fig, ((ax1, ax2), (ax3, ax4)) = plt.subplots(2, 2, figsize=(15, 12))\n        fig.suptitle('Análisis de Interpretabilidad Multimodal', fontsize=16, fontweight='bold')\n        \n        # Procesar datos de interpretabilidad por dominio\n        domains = ['musical', 'semantic']\n        domain_data = {}\n        \n        for domain in domains:\n            domain_results = interpretability_results.get(domain, {})\n            validations = domain_results.get('validations', [])\n            \n            if validations:\n                domain_data[domain] = {\n                    'global_scores': [v.get('global_interpretability_score', 0) for v in validations],\n                    'interpretable_ratios': [v.get('interpretability_ratio', 0) for v in validations],\n                    'cluster_counts': [v.get('n_clusters', 0) for v in validations],\n                    'classifications': [v.get('interpretability_classification', 'unknown') for v in validations]\n                }\n        \n        # 1. Distribución de scores de interpretabilidad\n        scores_data = []\n        for domain, data in domain_data.items():\n            for score in data['global_scores']:\n                scores_data.append({'Domain': domain.title(), 'Score': score})\n        \n        if scores_data:\n            scores_df = pd.DataFrame(scores_data)\n            sns.violinplot(data=scores_df, x='Domain', y='Score', ax=ax1)\n            ax1.set_title('Distribución Scores de Interpretabilidad Global')\n            ax1.set_ylabel('Score Interpretabilidad')\n            ax1.set_ylim(0, 1)\n            ax1.grid(True, alpha=0.3)\n        \n        # 2. Relación clusters vs interpretabilidad\n        cluster_vs_interp = []\n        for domain, data in domain_data.items():\n            for clusters, score in zip(data['cluster_counts'], data['global_scores']):\n                cluster_vs_interp.append({\n                    'Domain': domain.title(),\n                    'Clusters': clusters,\n                    'Interpretability': score\n                })\n        \n        if cluster_vs_interp:\n            cluster_df = pd.DataFrame(cluster_vs_interp)\n            \n            for domain in cluster_df['Domain'].unique():\n                domain_subset = cluster_df[cluster_df['Domain'] == domain]\n                ax2.scatter(domain_subset['Clusters'], domain_subset['Interpretability'], \n                           label=domain, s=60, alpha=0.7)\n            \n            ax2.set_title('Número de Clusters vs Interpretabilidad')\n            ax2.set_xlabel('Número de Clusters')\n            ax2.set_ylabel('Score Interpretabilidad')\n            ax2.legend()\n            ax2.grid(True, alpha=0.3)\n        \n        # 3. Distribución de clasificaciones\n        all_classifications = []\n        for domain, data in domain_data.items():\n            for classification in data['classifications']:\n                all_classifications.append({'Domain': domain.title(), 'Classification': classification})\n        \n        if all_classifications:\n            class_df = pd.DataFrame(all_classifications)\n            classification_counts = class_df.groupby(['Domain', 'Classification']).size().unstack(fill_value=0)\n            \n            classification_counts.plot(kind='bar', stacked=True, ax=ax3, \n                                     color=self.style_config['color_palette'][:len(classification_counts.columns)])\n            ax3.set_title('Distribución de Clasificaciones de Interpretabilidad')\n            ax3.set_xlabel('Dominio')\n            ax3.set_ylabel('Número de Configuraciones')\n            ax3.legend(title='Clasificación', bbox_to_anchor=(1.05, 1), loc='upper left')\n            ax3.tick_params(axis='x', rotation=0)\n        \n        # 4. Resumen comparativo\n        summary_data = []\n        summary = interpretability_results.get('summary', {})\n        \n        musical_avg = summary.get('average_interpretability_musical', 0)\n        semantic_avg = summary.get('average_interpretability_semantic', 0)\n        \n        if musical_avg > 0 or semantic_avg > 0:\n            summary_data = [musical_avg, semantic_avg]\n            domain_labels = ['Musical', 'Semántico']\n            \n            bars = ax4.bar(domain_labels, summary_data, \n                          color=self.style_config['color_palette'][:2])\n            ax4.set_title('Interpretabilidad Promedio por Dominio')\n            ax4.set_ylabel('Score Interpretabilidad Promedio')\n            ax4.set_ylim(0, 1)\n            \n            # Agregar valores en barras\n            for bar, value in zip(bars, summary_data):\n                height = bar.get_height()\n                ax4.text(bar.get_x() + bar.get_width()/2., height + 0.02,\n                        f'{value:.3f}', ha='center', va='bottom', fontweight='bold')\n            \n            # Línea de referencia \"bueno\" (0.6)\n            ax4.axhline(y=0.6, color='red', linestyle='--', alpha=0.7, label='Umbral \"Bueno\" (0.6)')\n            ax4.legend()\n        \n        plt.tight_layout()\n        \n        # Guardar figura\n        filename = f'interpretability_analysis_{pd.Timestamp.now().strftime(\"%Y%m%d_%H%M%S\")}.{self.figure_format}'\n        filepath = self.output_directory / filename\n        plt.savefig(filepath, dpi=self.figure_dpi, bbox_inches='tight')\n        plt.close()\n        \n        self.logger.info(f\"Análisis interpretabilidad guardado: {filepath}\")\n        return str(filepath)\n    \n    def generate_ranking_comparison_chart(self, ranking_results: Dict[str, Any]) -> str:\n        \"\"\"\n        Generar gráfico de ranking de configuraciones.\n        \n        Args:\n            ranking_results: Resultados de ranking\n            \n        Returns:\n            Ruta al archivo generado\n        \"\"\"\n        self.logger.info(\"Generando comparación de rankings\")\n        \n        fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(16, 8))\n        fig.suptitle('Ranking de Mejores Configuraciones por Dominio', fontsize=16, fontweight='bold')\n        \n        domains = ['musical', 'semantic']\n        \n        for i, domain in enumerate(domains):\n            ax = ax1 if i == 0 else ax2\n            domain_ranking = ranking_results.get(f'{domain}_ranking', {})\n            top_configs = domain_ranking.get('top_configurations', [])[:5]\n            \n            if not top_configs:\n                ax.text(0.5, 0.5, f'No hay configuraciones\\ndisponibles para {domain}',\n                       ha='center', va='center', transform=ax.transAxes, fontsize=12)\n                ax.set_title(f'Top 5 Configuraciones - {domain.title()}')\n                continue\n            \n            # Preparar datos\n            config_names = []\n            silhouette_scores = []\n            composite_scores = []\n            interpretability_scores = []\n            \n            for config in top_configs:\n                algorithm_name = config.get('algorithm_name', 'Unknown')\n                k_effective = config.get('k_effective', 0)\n                config_names.append(f\"{algorithm_name}\\n(K={k_effective})\")\n                silhouette_scores.append(config.get('silhouette_score', 0))\n                composite_scores.append(config.get('composite_score_partial', 0))\n                interpretability_scores.append(config.get('interpretability_score_global', 0))\n            \n            # Crear gráfico de barras múltiples\n            x = np.arange(len(config_names))\n            width = 0.25\n            \n            bars1 = ax.bar(x - width, silhouette_scores, width, label='Silhouette', \n                          color=self.style_config['color_palette'][0], alpha=0.8)\n            bars2 = ax.bar(x, composite_scores, width, label='Score Compuesto',\n                          color=self.style_config['color_palette'][1], alpha=0.8)\n            bars3 = ax.bar(x + width, interpretability_scores, width, label='Interpretabilidad',\n                          color=self.style_config['color_palette'][2], alpha=0.8)\n            \n            ax.set_title(f'Top 5 Configuraciones - {domain.title()}')\n            ax.set_xlabel('Configuración')\n            ax.set_ylabel('Score')\n            ax.set_xticks(x)\n            ax.set_xticklabels(config_names, fontsize=9)\n            ax.legend()\n            ax.grid(True, alpha=0.3, axis='y')\n            \n            # Agregar valores en las barras más altas (composite score)\n            for bar, score in zip(bars2, composite_scores):\n                height = bar.get_height()\n                ax.text(bar.get_x() + bar.get_width()/2., height + 0.01,\n                       f'{score:.3f}', ha='center', va='bottom', fontsize=8, fontweight='bold')\n        \n        plt.tight_layout()\n        \n        # Guardar figura\n        filename = f'ranking_comparison_{pd.Timestamp.now().strftime(\"%Y%m%d_%H%M%S\")}.{self.figure_format}'\n        filepath = self.output_directory / filename\n        plt.savefig(filepath, dpi=self.figure_dpi, bbox_inches='tight')\n        plt.close()\n        \n        self.logger.info(f\"Comparación ranking guardada: {filepath}\")\n        return str(filepath)\n    \n    def generate_complete_visualization_suite(self, experiment_results: Dict[str, Any]) -> Dict[str, str]:\n        \"\"\"\n        Generar suite completa de visualizaciones.\n        \n        Args:\n            experiment_results: Resultados completos de experimentación\n            \n        Returns:\n            Dict con rutas de archivos generados\n        \"\"\"\n        self.logger.info(\"Generando suite completa de visualizaciones\")\n        \n        generated_files = {}\n        \n        try:\n            # 1. Comparaciones algorítmicas por dominio\n            musical_comparison = self.generate_algorithm_comparison_chart(experiment_results, 'musical')\n            if musical_comparison:\n                generated_files['musical_algorithm_comparison'] = musical_comparison\n            \n            semantic_comparison = self.generate_algorithm_comparison_chart(experiment_results, 'semantic')\n            if semantic_comparison:\n                generated_files['semantic_algorithm_comparison'] = semantic_comparison\n            \n            # 2. Análisis cross-modal\n            cross_modal_data = experiment_results.get('cross_modal_analysis', {})\n            if cross_modal_data:\n                cross_modal_viz = self.generate_cross_modal_correspondence_matrix(cross_modal_data)\n                if cross_modal_viz:\n                    generated_files['cross_modal_correspondence'] = cross_modal_viz\n            \n            # 3. Análisis de interpretabilidad\n            interpretability_data = experiment_results.get('interpretability_analysis', {})\n            if interpretability_data:\n                interpretability_viz = self.generate_interpretability_analysis_chart(interpretability_data)\n                if interpretability_viz:\n                    generated_files['interpretability_analysis'] = interpretability_viz\n            \n            # 4. Ranking de configuraciones\n            ranking_data = experiment_results.get('ranking_results', {})\n            if ranking_data:\n                ranking_viz = self.generate_ranking_comparison_chart(ranking_data)\n                if ranking_viz:\n                    generated_files['ranking_comparison'] = ranking_viz\n            \n            # Generar reporte de visualizaciones\n            viz_report = self._generate_visualization_report(generated_files)\n            generated_files['visualization_report'] = viz_report\n            \n        except Exception as e:\n            self.logger.error(f\"Error generando visualizaciones: {e}\")\n            warnings.warn(f\"Error en generación de visualizaciones: {e}\")\n        \n        self.logger.info(f\"Suite de visualizaciones completada: {len(generated_files)} archivos\")\n        return generated_files\n    \n    def _generate_visualization_report(self, generated_files: Dict[str, str]) -> str:\n        \"\"\"\n        Generar reporte de visualizaciones generadas.\n        \n        Args:\n            generated_files: Dict con archivos generados\n            \n        Returns:\n            Ruta al reporte generado\n        \"\"\"\n        report_content = {\n            'visualization_suite_timestamp': pd.Timestamp.now().strftime('%Y%m%d_%H%M%S'),\n            'total_visualizations_generated': len(generated_files),\n            'generated_files': generated_files,\n            'visualization_descriptions': {\n                'musical_algorithm_comparison': 'Análisis comparativo de algoritmos en dominio musical (12D)',\n                'semantic_algorithm_comparison': 'Análisis comparativo de algoritmos en dominio semántico (384D)',\n                'cross_modal_correspondence': 'Matriz de correspondencias entre clustering musical y semántico',\n                'interpretability_analysis': 'Análisis de interpretabilidad y granularidad explicativa',\n                'ranking_comparison': 'Ranking de mejores configuraciones por dominio'\n            }\n        }\n        \n        report_filename = f'visualization_report_{report_content[\"visualization_suite_timestamp\"]}.json'\n        report_filepath = self.output_directory / report_filename\n        \n        with open(report_filepath, 'w', encoding='utf-8') as f:\n            json.dump(report_content, f, indent=2, ensure_ascii=False)\n        \n        self.logger.info(f\"Reporte visualizaciones guardado: {report_filepath}\")\n        return str(report_filepath)\n\n\n# Función de conveniencia\ndef generate_multimodal_visualizations(experiment_results: Dict[str, Any],\n                                     output_directory: str,\n                                     figure_format: str = 'png',\n                                     verbose: bool = True) -> Dict[str, str]:\n    \"\"\"\n    Función de conveniencia para generar visualizaciones completas.\n    \n    Args:\n        experiment_results: Resultados de experimentación\n        output_directory: Directorio de salida\n        figure_format: Formato de figuras\n        verbose: Logging detallado\n        \n    Returns:\n        Dict con archivos generados\n    \"\"\"\n    viz_generator = VisualizationGenerator(\n        output_directory=output_directory,\n        figure_format=figure_format,\n        verbose=verbose\n    )\n    \n    return viz_generator.generate_complete_visualization_suite(experiment_results)"
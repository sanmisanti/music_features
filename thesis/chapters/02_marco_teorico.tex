\chapter{Marco Teórico}
\label{ch:marco-teorico}

% ==========================================================================
% NOTA DE PLANIFICACIÓN (eliminar al redactar)
%
% Este capítulo define las herramientas formales utilizadas en el proyecto.
% Diferencia con el Estado de la Cuestión (Cap. 2): el EdlC presenta QUÉ
% se ha investigado y POR QUÉ importa; el Marco Teórico presenta CÓMO
% funcionan las técnicas que este trabajo utiliza (con fórmulas, definiciones
% formales, y condiciones de aplicabilidad).
%
% Criterio de inclusión: un concepto aparece aquí si y solo si se usa
% directamente en alguna etapa del pipeline. No se incluyen técnicas
% descartadas ni panoramas generales sin aplicación concreta.
%
% Convención de anotación por sección:
%   CONCEPTO    — qué explica formalmente esta sección
%   USADO EN    — módulos de código y secciones de Solución Propuesta (Cap. 5)
%   REFERENCIAS — claves de bibliography.bib disponibles
%   INVESTIGACIÓN — documentos de thesis/investigacion/ relevantes
%   CONTENIDO   — qué debería incluir la redacción
% ==========================================================================

% ==========================================================================
% SECCIÓN 4.1 — Sistemas de Recomendación Musical
% ==========================================================================
\section{Sistemas de Recomendación Musical}
\label{sec:sistemas-recomendacion}

% CONCEPTO:    Paradigmas de recomendación (colaborativo, contenido, híbrido)
%              y sus limitaciones en el dominio musical.
% USADO EN:    §5.7 Sistema de Recomendación Híbrido
%              → recommendation_system/scripts/ (Etapa 5)
% REFERENCIAS: burke_2002_hybrid, schedl_2018_challenges_mrs,
%              sarwar_2001_itembased, koren_2009_mf
% INVESTIGACIÓN: 01_sistemas_recomendacion.md (23 fuentes)
% CONTENIDO:   Definición formal de recomendación como problema de ranking.
%              Filtrado colaborativo (user-based, item-based, factorización).
%              Filtrado por contenido (representación de items, similitud).
%              Sistemas híbridos (taxonomía de Burke: weighted, switching,
%              mixed, feature combination, cascade, meta-level).
%              Cold-start, sparsity, escalabilidad.
%              Particularidades del dominio musical: preferencia implícita,
%              secuencialidad, contexto de escucha.

\subsection{Filtrado Colaborativo y por Contenido}
\label{subsec:paradigmas-recomendacion}

% CONCEPTO:    Definiciones formales de ambos paradigmas y sus variantes.
% USADO EN:    Justificación de por qué el sistema es content-based
%              (no hay datos de interacción usuario-canción en el dataset).
% REFERENCIAS: sarwar_2001_itembased, koren_2009_mf, hu_2008_implicit_feedback,
%              he_2017_ncf
% CONTENIDO:   Matriz de interacciones, factorización matricial, similitud
%              coseno entre perfiles de items. Limitación clave: el dataset
%              no contiene historial de escucha → colaborativo no es aplicable.

\subsection{Sistemas Híbridos}
\label{subsec:sistemas-hibridos}

% CONCEPTO:    Combinación de múltiples fuentes de información para recomendación.
% USADO EN:    §5.7 Estrategia de Fusión y Optimización de Pesos (Etapa 5)
% REFERENCIAS: burke_2002_hybrid, anelli_2024_hybrid_gnn_music
% CONTENIDO:   Taxonomía de Burke. El sistema propuesto combina dos espacios
%              de contenido (semántico + musical) → es un híbrido de tipo
%              "feature combination" dentro del paradigma content-based.

\subsection{Evaluación de Sistemas de Recomendación}
\label{subsec:evaluacion-recomendacion}

% CONCEPTO:    Métricas de evaluación: precisión, diversidad, cobertura,
%              serendipity. Evaluación offline vs online.
% USADO EN:    §5.7 Optimización de Pesos (Etapa 5)
%              → Precision@K, diversidad intra-lista
% REFERENCIAS: zangerle_2022_fevr, castells_2022_offline,
%              kaminskas_2016_beyond, klimashevskaia_2024_popularity
% INVESTIGACIÓN: 06_evaluacion_experimental.md (20 fuentes)
% CONTENIDO:   Precision@K, Recall@K, nDCG. Limitaciones de evaluación
%              offline con proxy labels (género como ground truth).
%              Beyond-accuracy: diversidad, novedad, cobertura de catálogo.
%              Sesgo de popularidad en evaluación.

% ==========================================================================
% SECCIÓN 4.2 — Procesamiento de Lenguaje Natural aplicado a Letras
% ==========================================================================
\section{Procesamiento de Lenguaje Natural aplicado a Letras}
\label{sec:nlp-musica}

% CONCEPTO:    Pipeline completo de NLP para letras musicales:
%              representación vectorial → modelos de lenguaje → embeddings.
% USADO EN:    §5.3 Componente Semántico: Vectorización de Letras
%              → src/features/vectorizer.py
% INVESTIGACIÓN: 02_music_information_retrieval.md (21 fuentes),
%                03_nlp_bert_embeddings.md (18 fuentes)

\subsection{Representación Vectorial de Texto}
\label{subsec:representacion-vectorial}

% CONCEPTO:    Evolución de representaciones: BoW → TF-IDF → word embeddings
%              → embeddings contextuales. Por qué cada paso supera al anterior.
% USADO EN:    Justificación de por qué no usar BoW/TF-IDF para letras
%              (pierden orden, semántica, contexto).
% REFERENCIAS: mikolov_2013_word2vec, bojanowski_2017_fasttext
% CONTENIDO:   Bag-of-words y sus limitaciones (dimensionalidad, sparsity,
%              independencia de términos). Word2Vec: CBOW y Skip-gram,
%              propiedad de analogía. FastText: subword como extensión.
%              Limitación compartida: un vector por palabra, sin contexto
%              (polisemia no resuelta). Esto motiva los modelos contextuales.

\subsection{Arquitectura Transformer y Mecanismo de Atención}
\label{subsec:transformer}

% CONCEPTO:    Self-attention, multi-head attention, positional encoding.
%              La base arquitectónica de BERT y E5.
% USADO EN:    Comprensión de cómo E5 procesa texto internamente.
%              La ventana de 512 tokens es un límite de esta arquitectura.
% REFERENCIAS: vaswani_2017_attention
% CONTENIDO:   Atención como función Q, K, V. Self-attention: cada token
%              atiende a todos los demás (complejidad O(n²), de ahí el
%              límite de ventana). Multi-head: múltiples espacios de
%              atención paralelos. Positional encoding: cómo el modelo
%              sabe el orden de las palabras. Encoder vs decoder.
%              Fórmula de scaled dot-product attention.

\subsection{BERT y Modelos Bidireccionales}
\label{subsec:bert}

% CONCEPTO:    Pre-entrenamiento bidireccional, MLM, fine-tuning.
%              La diferencia clave respecto a modelos unidireccionales.
% USADO EN:    E5 se basa en arquitectura BERT (encoder-only).
%              §5.3.1 Selección del Modelo de Embeddings.
% REFERENCIAS: devlin_2019_bert
% CONTENIDO:   Masked Language Model (MLM): predicción de tokens ocultos.
%              Next Sentence Prediction (NSP). Bidireccionalidad: cada token
%              tiene contexto izquierdo Y derecho (vs GPT que solo ve
%              izquierda). Token [CLS] como representación de secuencia.
%              Limitación: [CLS] sin fine-tuning produce embeddings de
%              baja calidad para similitud → motiva Sentence-BERT.

\subsection{Sentence Embeddings: de Sentence-BERT a E5}
\label{subsec:sentence-embeddings}

% CONCEPTO:    Embeddings a nivel de oración/párrafo. Redes siamesas,
%              contrastive learning, instrucciones de tarea.
% USADO EN:    §5.3.1 Selección del Modelo — justificación de E5.
%              src/features/vectorizer.py: prefijo "passage: ", mean pooling.
% REFERENCIAS: reimers_2019_sentencebert, reimers_2020_multilingual,
%              wang_2022_e5, wang_2020_minilm
% CONTENIDO:   Sentence-BERT: fine-tuning con redes siamesas y función
%              de pérdida coseno. MiniLM: destilación de atención para
%              modelos compactos (128 tokens). E5: pre-entrenamiento
%              contrastivo débilmente supervisado con pares (query, passage)
%              de la web. Prefijo de tarea ("query: ", "passage: ") como
%              mecanismo de instrucción. Multilingual: distilación cross-lingual.
%              Por qué 384D es suficiente para clustering musical.

\subsection{Tokenización Subword}
\label{subsec:tokenizacion}

% CONCEPTO:    Algoritmos de tokenización (BPE, WordPiece, SentencePiece).
%              Por qué 1 palabra ≠ 1 token, especialmente en texto multilingüe.
% USADO EN:    §5.3.2 Análisis de Cobertura de Tokens.
%              La discrepancia entre heurística (58.8%) y real (53.48%).
%              src/features/vectorizer.py: measure_token_coverage().
% REFERENCIAS: devlin_2019_bert (WordPiece), bojanowski_2017_fasttext (subword)
% CONTENIDO:   Vocabulario finito vs vocabulario abierto. BPE: merge iterativo
%              de pares frecuentes. WordPiece: similar pero con criterio de
%              likelihood. Consecuencia: palabras infrecuentes (slang, idiomas
%              minoritarios, ortografía no estándar de letras) se fragmentan
%              en más tokens. Implicación directa: ratio tokens/palabra mayor
%              en letras musicales que en texto estándar.

% ==========================================================================
% SECCIÓN 4.3 — Features Musicales [YA ESCRITO]
% ==========================================================================
%
% CONCEPTO:    Los 12 descriptores acústicos de Spotify, sus escalas,
%              validez perceptual y limitaciones.
% USADO EN:    §5.2.1 Fuentes de Datos, §5.4 Componente Musical.
%              src/features/normalizer.py: MUSICAL_FEATURES (12 features).
%              src/config.py: MUSICAL_FEATURES list.
% REFERENCIAS: spotify_2024_web_api, ullmann_2022_validation
% INVESTIGACIÓN: 07_ingenieria_datos_features.md (22 fuentes)
% ESTADO:      COMPLETO — no modificar.
%
\section{Features Musicales}
\label{sec:features-musicales}

La caracterización computacional de una canción requiere transformar una señal de audio ---esencialmente una secuencia de variaciones de presión sonora--- en un conjunto de descriptores numéricos que capturen propiedades relevantes para la tarea en cuestión. En el contexto de los sistemas de recomendación y el clustering musical, estos descriptores deben capturar tanto propiedades físicas objetivas de la señal (volumen, velocidad) como propiedades perceptuales que reflejen la experiencia subjetiva del oyente (bailabilidad, valencia emocional, nivel de energía). La presente sección describe formalmente los descriptores utilizados en este trabajo, sus escalas de medición, las consideraciones de validez perceptual documentadas en la literatura, y las implicaciones de la opacidad algorítmica de su fuente.

% ---------------------------------------------------------------------------
% SUBSECCIÓN 4.3.1 — Descriptores Acústicos de Spotify
% ---------------------------------------------------------------------------
\subsection{Descriptores Acústicos de Spotify}
\label{subsec:descriptores-spotify}

La Web API de Spotify ha proporcionado históricamente un conjunto de 12 descriptores acústicos computacionales para cada pista de su catálogo \parencite{spotify_2024_web_api}. Estas features se originaron en The Echo Nest, una empresa de análisis musical adquirida por Spotify en 2014, cuya tecnología propietaria permanece como caja negra: Spotify nunca ha publicado la metodología exacta ni los modelos subyacentes utilizados para computar estos valores. La documentación oficial se limita a descripciones funcionales de alto nivel, lo cual genera consideraciones legítimas sobre la reproducibilidad y la interpretabilidad de los resultados derivados de estas features.

Los 12 descriptores se organizan en tres categorías según su naturaleza y escala de medición.

\subsubsection*{Índices perceptuales}

Siete de las doce features son índices normalizados en el rango $[0, 1]$ que cuantifican propiedades perceptuales de la señal de audio:

\begin{description}[leftmargin=2em, style=nextline]
    \item[\textit{Danceability}] Mide cuán adecuada es una pista para el baile, combinando información de tempo, estabilidad rítmica, fuerza del beat y regularidad general. Un valor de 0 indica una pista sin cualidades rítmicas bailables; un valor de 1, máxima bailabilidad.

    \item[\textit{Energy}] Cuantifica la intensidad y actividad perceptual de la pista. Los factores que contribuyen incluyen el rango dinámico, el loudness percibido, el timbre, la tasa de onsets ---la frecuencia con que se producen nuevos eventos sonoros--- y la entropía general de la señal. Una grabación de Bach para clavecín tendría un valor bajo; una pista de metal, un valor alto.

    \item[\textit{Speechiness}] Detecta la presencia de voz hablada (en contraposición a voz cantada o contenido instrumental). Spotify establece tres franjas: valores superiores a 0.66 indican contenido predominantemente hablado (podcasts, audiolibros); entre 0.33 y 0.66, una mezcla de música y habla; por debajo de 0.33, contenido musical cantado o instrumental.

    \item[\textit{Acousticness}] Estima la probabilidad de que la grabación sea acústica ---es decir, producida sin amplificación electrónica ni procesamiento digital significativo---. Un valor cercano a 1 indica alta confianza de que la pista es acústica.

    \item[\textit{Instrumentalness}] Predice si una pista carece de contenido vocal. Spotify utiliza 0.5 como umbral: valores superiores se interpretan como pistas probablemente instrumentales. En la práctica, la distribución de este descriptor es fuertemente bimodal: la mayoría de las canciones se concentran cerca de 0 (vocales presentes) o cerca de 1 (instrumental).

    \item[\textit{Liveness}] Detecta la presencia de audiencia en la grabación. Valores superiores a 0.8 sugieren alta probabilidad de que la pista sea una grabación en vivo.

    \item[\textit{Valence}] Cuantifica la positividad emocional transmitida por la música. Valores altos corresponden a sonidos percibidos como alegres, eufóricos o animados; valores bajos, a sonidos percibidos como tristes, melancólicos o tensos. De todos los índices perceptuales, la valencia es probablemente el más subjetivo y el más difícil de validar empíricamente.
\end{description}

\subsubsection*{Propiedades musicales}

Tres features capturan propiedades musicológicas con escalas específicas de su dominio:

\begin{description}[leftmargin=2em, style=nextline]
    \item[\textit{Key}] La tonalidad estimada de la pista, codificada como un entero de 0 a 11 según la notación de clase de altura (0 = Do, 1 = Do\#, \ldots, 11 = Si). Un valor de $-1$ indica que la tonalidad no pudo ser detectada. Esta codificación numérica es ordinal en apariencia pero cíclica en naturaleza ---Do y Si son tonalidades adyacentes en el círculo de quintas, aunque numéricamente distantes (0 y 11)---. Este carácter cíclico debe contemplarse al utilizar la feature en algoritmos basados en distancia euclidiana.

    \item[\textit{Mode}] La modalidad de la pista: 1 para modo mayor, 0 para modo menor. Es una variable binaria que, al igual que \textit{key}, presenta desafíos cuando se trata como feature continua en algoritmos de clustering.

    \item[\textit{Tempo}] La velocidad estimada de la pista en BPM (beats per minute). A diferencia de los índices perceptuales, el tempo tiene unidades físicas y su rango práctico en música popular se extiende entre aproximadamente 60 y 200 BPM. Los algoritmos de detección de tempo pueden cometer errores de factor 2 ---identificar el doble o la mitad del tempo real---, una limitación conocida en la literatura de MIR.
\end{description}

\subsubsection*{Medición técnica}

Dos features restantes corresponden a mediciones técnicas directas:

\begin{description}[leftmargin=2em, style=nextline]
    \item[\textit{Loudness}] El volumen promedio de la pista medido en decibelios (dB), típicamente entre $-60$ y 0~dB. A diferencia de los índices perceptuales normalizados, esta feature opera en una escala logarítmica: una diferencia de 10~dB corresponde aproximadamente a una duplicación de la intensidad percibida. La tendencia de la industria musical hacia niveles de loudness cada vez mayores ---la denominada \textit{loudness war}--- introduce un sesgo temporal: canciones más recientes tienden a exhibir valores más altos.

    \item[\textit{Duration\_ms}] La duración de la pista en milisegundos. Es la única feature cuyo valor proviene directamente de los metadatos del archivo de audio, sin procesamiento algorítmico. Su escala (valores típicos entre 100\,000 y 400\,000) difiere en varios órdenes de magnitud respecto a los índices perceptuales, lo que requiere normalización antes de utilizarla en algoritmos basados en distancia.
\end{description}

\subsubsection*{Validez perceptual y limitaciones}

La pregunta de si estos descriptores reflejan fielmente la percepción humana ha sido abordada por múltiples estudios con resultados mixtos. Investigaciones recientes han encontrado que \textit{danceability} y \textit{loudness} son predictores confiables de la valencia percibida por los oyentes, mientras que \textit{acousticness} y \textit{danceability} predicen consistentemente el nivel de activación emocional (\textit{arousal}). El tempo óptimo para regulación del estado de ánimo a través del movimiento corporal se ubica alrededor de 120~BPM. Sin embargo, la validación empírica también ha revelado limitaciones: se ha documentado que \textit{danceability} no correlaciona significativamente con la percepción subjetiva de cuán bailable es una canción para un oyente particular, lo que sugiere que el descriptor captura propiedades acústicas objetivas (regularidad rítmica, fuerza del beat) pero no la respuesta individual al estímulo.

Estas consideraciones definen el marco dentro del cual los 12 descriptores deben interpretarse en el presente trabajo: constituyen una aproximación útil a las cualidades perceptuales de la música, pero no una representación fiel de la experiencia subjetiva del oyente. Las features que operan en escalas heterogéneas (\textit{loudness} en dB, \textit{tempo} en BPM, \textit{duration\_ms} en milisegundos, índices en $[0,1]$) requieren normalización antes de su uso conjunto en algoritmos de distancia. Adicionalmente, \textit{key} y \textit{mode} son variables categóricas codificadas numéricamente cuyo tratamiento como features continuas introduce relaciones de magnitud artificiales. Estas cuestiones técnicas se abordan en la descripción del pipeline de datos (Capítulo~\ref{ch:solucion-propuesta}).

Finalmente, cabe señalar que en noviembre de 2024 Spotify restringió el acceso a los endpoints de audio features de su Web API para nuevas aplicaciones \parencite{spotify_2024_web_api}. El dataset utilizado en este trabajo fue recopilado antes de esta restricción, por lo que los datos son válidos; sin embargo, investigadores futuros que deseen replicar o extender este trabajo no podrán obtener estas features mediante la misma vía. Esta dependencia de una fuente propietaria constituye una limitación que se discute en las conclusiones.

% ==========================================================================
% SECCIÓN 4.4 — Normalización de Features
% ==========================================================================
\section{Normalización de Features}
\label{sec:normalizacion-teoria}

% CONCEPTO:    Técnicas de normalización para hacer comparables features
%              en escalas heterogéneas. Estandarización z-score y
%              normalización L2 en espacios de embeddings.
% USADO EN:    §5.4 Componente Musical (z-score sobre 12 features).
%              §5.3.3 Estrategia de Chunking (re-normalización L2).
%              src/features/normalizer.py, src/features/vectorizer.py.
% REFERENCIAS: montague_2001_normalization
% INVESTIGACIÓN: 07_ingenieria_datos_features.md
% CONTENIDO:   Por qué normalizar: la distancia euclidiana es sensible a
%              escala → features con mayor rango dominan el cálculo.

\subsection{Estandarización Z-Score}
\label{subsec:zscore-teoria}

% CONCEPTO:    Transformación z = (x - μ) / σ. Propiedades: media 0, DE 1.
%              Preserva la forma de la distribución (no acota rango).
% USADO EN:    §5.4 eq. (1). src/features/normalizer.py: normalize_features().
%              Aplicado a las 12 features musicales → [17964, 12] float32.
% CONTENIDO:   Fórmula, interpretación geométrica (centrar y escalar),
%              supuestos (no requiere normalidad, pero outliers extremos
%              comprimen el rango útil). Alternativas descartadas:
%              min-max (sensible a outliers), robust scaler (basado en
%              mediana/IQR). Justificación de z-score: interpretabilidad,
%              amplio uso en clustering, sin acotamiento artificial.

\subsection{Normalización L2 en Espacios de Embeddings}
\label{subsec:l2-teoria}

% CONCEPTO:    Proyección a la hiperesfera unitaria: v̂ = v / ||v||₂.
%              Relación entre similitud coseno y distancia euclidiana
%              sobre vectores L2-normalizados.
% USADO EN:    §5.3.3 re-normalización post-chunking.
%              src/features/vectorizer.py: vectorize_lyrics() normaliza L2.
%              Todos los embeddings semánticos viven en S^383.
% CONTENIDO:   Fórmula de norma L2. Propiedad clave: si ||u|| = ||v|| = 1,
%              entonces d²(u,v) = 2(1 - cos(u,v)). Esto hace que K-Means
%              (que minimiza distancia euclidiana) sea equivalente a
%              maximizar similitud coseno. Implicación: clustering con
%              K-Means sobre embeddings L2-normalizados agrupa por
%              similitud semántica.

\subsection{Variables Categóricas en Espacios Continuos}
\label{subsec:categoricas-continuas}

% CONCEPTO:    Problema de tratar key (0-11, cíclica) y mode (0/1, binaria)
%              como continuas. Alternativas: one-hot, encoding cíclico.
% USADO EN:    §5.4 — documentado como limitación conocida.
%              src/features/normalizer.py: key y mode pasan por z-score.
% CONTENIDO:   Naturaleza cíclica de key (Do y Si son adyacentes, pero
%              0 y 11 son numéricamente distantes). Encoding cíclico
%              (sin(2πk/12), cos(2πk/12)) como alternativa teórica.
%              One-hot: agrega 12+ dimensiones a un espacio de 12D.
%              Justificación pragmática: en 12 features, el impacto de
%              key y mode es limitado por la maldición de dimensionalidad
%              inversa (pocas dimensiones → cada una importa más, pero
%              el clustering opera sobre 12+384D donde 2 dimensiones
%              mal codificadas tienen peso marginal).

% ==========================================================================
% SECCIÓN 4.5 — Análisis de Clustering
% ==========================================================================
\section{Análisis de Clustering}
\label{sec:clustering-teoria}

% CONCEPTO:    Marco teórico completo de clustering: verificación de
%              estructura, algoritmos, evaluación y refinamiento.
% USADO EN:    §5.6 Clustering Multimodal (Etapa 4, pendiente).
%              → código pendiente en src/clustering/ (Etapa 4).
% INVESTIGACIÓN: 04_clustering_algoritmos.md (25 fuentes)

\subsection{Tendencia al Agrupamiento: Estadístico de Hopkins}
\label{subsec:hopkins}

% CONCEPTO:    Test estadístico para verificar si los datos tienen
%              estructura de clusters antes de aplicar algoritmos.
% USADO EN:    §5.6.1 Evaluación de Tendencia al Agrupamiento.
%              → se aplicará sobre ambos espacios (384D y 12D) por separado.
%              src/config.py: HOPKINS_ITERATIONS=30, HOPKINS_SAMPLE_SIZE=100.
% REFERENCIAS: (necesita entrada bib — Lawson & Jurs 1990 o Banerjee & Dave 2004)
% CONTENIDO:   Definición: H = Σuᵢ / (Σuᵢ + Σwᵢ) donde u = distancias
%              de puntos uniformes al vecino más cercano, w = distancias
%              de puntos reales al vecino más cercano. H > 0.5 indica
%              estructura. Interpretación: H~0.5 = aleatorio, H~0.7 =
%              tendencia moderada, H>0.75 = tendencia alta. Bootstrap
%              con N iteraciones para intervalo de confianza.
%              Sensibilidad a dimensionalidad (curse of dimensionality).

\subsection{K-Means y K-Means++}
\label{subsec:kmeans}

% CONCEPTO:    Algoritmo de partición por centroides. Inicialización
%              inteligente. Convergencia y óptimos locales.
% USADO EN:    §5.6.2 Selección de Algoritmo — candidato principal.
%              src/config.py: CLUSTERING_K_RANGE = [5, 6, 7, 8].
% REFERENCIAS: arthur_2007_kmeans_plus_plus
% CONTENIDO:   Función objetivo: minimizar WCSS (within-cluster sum of
%              squares). Algoritmo de Lloyd: asignación → actualización →
%              repetir. Convergencia garantizada pero a óptimo local.
%              K-Means++: inicialización proporcional a D² — reduce
%              varianza entre ejecuciones y mejora WCSS final.
%              Complejidad O(nkdi), donde n=muestras, k=clusters,
%              d=dimensiones, i=iteraciones. Supuesto implícito:
%              clusters convexos e isotrópicos. Sensibilidad a outliers.

\subsection{Clustering Jerárquico: Método de Ward}
\label{subsec:ward}

% CONCEPTO:    Aglomeración bottom-up con criterio de mínima varianza.
%              Dendrograma y selección de corte.
% USADO EN:    §5.6.2 — alternativa a K-Means, evaluada en mismos K.
% REFERENCIAS: ward_1963_hierarchical, murtagh_2014_ward
% CONTENIDO:   Criterio de Ward: merge que minimiza incremento de WCSS
%              total. Equivalente a minimizar la varianza intra-cluster.
%              IMPORTANTE: dos implementaciones distintas (Ward1 vs Ward2,
%              Murtagh & Legendre 2014) — scipy usa Ward2 (Lance-Williams).
%              Dendrograma como representación jerárquica completa.
%              Ventaja: no requiere K a priori (se elige post-hoc).
%              Desventaja: O(n²) en espacio, greedy (merge irreversible).

\subsection{Clustering Basado en Densidad: HDBSCAN}
\label{subsec:hdbscan}

% CONCEPTO:    Extensión jerárquica de DBSCAN. No requiere K ni epsilon
%              fijos. Detecta clusters de forma y densidad arbitraria.
% USADO EN:    §5.6.2 — tercer algoritmo candidato.
%              src/config.py: DBSCAN_EPS_RANGE (para comparación).
% REFERENCIAS: campello_2013_hdbscan
% CONTENIDO:   DBSCAN: core points, border points, noise. Eps y MinPts.
%              Limitación: un solo nivel de densidad.
%              HDBSCAN: jerarquía de densidades, extracción de clusters
%              estables via excess of mass. Parámetro min_cluster_size.
%              Ventaja clave: descarta noise points (no fuerza asignación).
%              Desventaja: puede descartar muchos puntos como ruido en
%              datos de alta dimensionalidad.

\subsection{Evaluación Interna de Clustering}
\label{subsec:metricas-clustering}

% CONCEPTO:    Métricas que evalúan calidad de clusters sin ground truth
%              externo. Silhouette, Davies-Bouldin, Calinski-Harabasz.
% USADO EN:    §5.6.2 — selección del mejor algoritmo/K.
%              src/config.py: OBJECTIVE_WEIGHTS (silhouette: 0.30, etc.)
% REFERENCIAS: rousseeuw_1987_silhouettes, chicco_2025_silhouette_db
% CONTENIDO:   Silhouette: s(i) = (b(i) - a(i)) / max(a(i), b(i)).
%              Rango [-1, 1], interpretación. Promedio sobre todos los
%              puntos. Davies-Bouldin: ratio de dispersión intra vs
%              separación inter. Menor = mejor.
%              Chicco et al. (2025): Silhouette + DB son más informativos
%              que Calinski-Harabasz para clusters convexos.
%              Limitación: todas asumen clusters convexos (sesgo contra
%              clusters de forma arbitraria).

\subsection{Maldición de la Dimensionalidad}
\label{subsec:curse-dimensionality}

% CONCEPTO:    Degradación de métricas de distancia en alta dimensionalidad.
%              Concentración de distancias. Implicaciones para clustering.
% USADO EN:    Justificación de por qué 384D es un desafío para clustering.
%              §5.6.1 — Hopkins puede verse afectado en alta dimensionalidad.
%              Justificación de evaluación separada (384D vs 12D).
% REFERENCIAS: aggarwal_2001_dimensionality
% CONTENIDO:   Fenómeno: en alta dimensionalidad, la diferencia relativa
%              entre la distancia al vecino más cercano y al más lejano
%              tiende a 0. Fórmula de Aggarwal et al. para distancias Lp.
%              Implicación: K-Means puede degradar porque las asignaciones
%              a centroides se vuelven menos discriminativas.
%              Mitigación parcial: L2-normalización confina los puntos
%              a una hiperesfera, lo que preserva la discriminabilidad
%              angular (similitud coseno).

\subsection{Reducción Dimensional: UMAP}
\label{subsec:umap}

% CONCEPTO:    Reducción no lineal de dimensionalidad para visualización
%              y (opcionalmente) para mejorar clustering.
% USADO EN:    Visualización de clusters en 2D (Etapa 4).
%              Posible reducción pre-clustering si Hopkins es bajo en 384D.
% REFERENCIAS: mcinnes_2018_umap, allaoui_2020_umap_clustering
% CONTENIDO:   UMAP: construcción de grafo de vecinos en alta dimensión,
%              optimización de layout en baja dimensión (cross-entropy).
%              Hiperparámetros: n_neighbors (local vs global), min_dist
%              (compacidad), n_components (2D para visualización, >2 para
%              clustering). Ventaja sobre t-SNE: preserva mejor estructura
%              global, determinístico con seed, más rápido.
%              Allaoui et al. (2020): UMAP como preprocesamiento mejora
%              clustering en datos de alta dimensionalidad.

\subsection{Purificación de Clusters}
\label{subsec:purificacion-teoria}

% CONCEPTO:    Refinamiento post-clustering: eliminación o reasignación
%              de puntos mal asignados para mejorar cohesión.
% USADO EN:    §5.6.3 Purificación de Clusters (Etapa 4).
%              scripts/cluster_purification.py (referencia v1).
% REFERENCIAS: dinh_2021_clustering_refinement
% CONTENIDO:   Motivación: los algoritmos de clustering cometen errores de
%              asignación, especialmente en zonas fronterizas entre clusters.
%              Estrategias: umbral de silhouette por punto (eliminar puntos
%              con s(i) < umbral), reasignación iterativa (re-asignar al
%              cluster más cercano si mejora silhouette global).
%              Hallazgo de investigación: NO existe framework formal para
%              purificación — es una contribución original de este trabajo.
%              Métricas pre/post purificación para cuantificar mejora.

% ==========================================================================
% SECCIÓN 4.6 — Representaciones Multimodales y Fusión
% ==========================================================================
\section{Representaciones Multimodales y Fusión}
\label{sec:representaciones-multimodales}

% CONCEPTO:    Combinación de información de múltiples modalidades (texto
%              + audio) en un sistema unificado. Taxonomía de fusión.
% USADO EN:    §5.5 Unificación de Representaciones, §5.7 Estrategia de Fusión.
%              src/features/unifier.py: build_unified_dataset().
%              Etapa 5: fusión ponderada de scores semánticos y musicales.
% INVESTIGACIÓN: 05_fusion_multimodal.md (18 fuentes)

\subsection{Taxonomía de Fusión Multimodal}
\label{subsec:taxonomia-fusion}

% CONCEPTO:    Fusión temprana (feature-level), intermedia, y tardía
%              (decision-level). Trade-offs de cada estrategia.
% USADO EN:    Justificación de la arquitectura: se usa fusión tardía
%              (scores independientes por modalidad, combinados al final)
%              porque las modalidades tienen dimensionalidades muy
%              diferentes (384D vs 12D).
% REFERENCIAS: baltrusaitis_2019_multimodal, liang_2024_foundations
% CONTENIDO:   Fusión temprana: concatenar features → un solo modelo.
%              Problema con 384D + 12D: las 384 dimensiones semánticas
%              ahogarían las 12 musicales por mayoría numérica.
%              Fusión tardía: modelos/scores independientes por modalidad,
%              combinados al final. Permite ponderar contribución relativa.
%              Fusión intermedia: representaciones compartidas aprendidas
%              (contrastive learning, joint embeddings). Requiere
%              entrenamiento end-to-end — fuera del alcance de este trabajo.

\subsection{Fusión por Combinación Lineal de Scores}
\label{subsec:fusion-lineal}

% CONCEPTO:    score_final = α · score_semántico + (1-α) · score_musical.
%              Normalización de scores, optimización de α.
% USADO EN:    §5.7.1 Estrategia de Fusión, §5.7.2 Optimización de Pesos.
%              src/config.py: OBJECTIVE_WEIGHTS.
% REFERENCIAS: vogt_1999_linear_fusion, montague_2001_normalization
% CONTENIDO:   Fórmula de combinación lineal. Requisito: scores comparables
%              (misma escala, misma distribución). Normalización de scores:
%              min-max, z-score, rank-based. Búsqueda de α óptimo:
%              grid search sobre rango [0,1] con métrica de evaluación.
%              Interpretación de α: peso relativo de cada modalidad en
%              la recomendación final.

\subsection{Complementariedad entre Modalidades}
\label{subsec:complementariedad}

% CONCEPTO:    Medir cuánta información nueva aporta una modalidad
%              respecto a la otra. NMI como métrica de solapamiento.
% USADO EN:    Evaluación cross-modal (Etapa 4 y 5).
%              Benchmark v1: NMI cross-modal = 0.0567 (alta complementariedad).
% REFERENCIAS: oramas_2018_multimodal_genre, liyanarachchi_2025_multimodal_mer,
%              manco_2022_muscall, huang_2022_mulan
% CONTENIDO:   NMI(C_semántico, C_musical): si los clusters semánticos y
%              musicales son muy diferentes (NMI bajo), las modalidades
%              capturan información complementaria → la fusión agrega valor.
%              Si NMI es alto, ambas modalidades dicen lo mismo → fusión
%              aporta poco. Fórmula de NMI: MI(U,V) / √(H(U)·H(V)).
%              Interpretación en el contexto musical: las letras capturan
%              temática, el audio captura sonoridad → alta complementariedad
%              es esperable y deseable.
